import { cleanEnv, str, url } from 'envalid';

const rawEnv = {
  NODE_ENV: process.env.NEXT_PUBLIC_NODE_ENV,
  API_BASEURL: process.env.NEXT_PUBLIC_API_BASEURL,
  SUBGRAPH_MONAD_URL: process.env.NEXT_PUBLIC_SUBGRAPH_MONAD_URL,

  MONAD_POOL_ADDRESSES_PROVIDER: process.env.NEXT_PUBLIC_MONAD_POOL_ADDRESSES_PROVIDER,
  MONAD_POOL: process.env.NEXT_PUBLIC_MONAD_POOL,
  MONAD_POOL_CONFIGURATOR: process.env.NEXT_PUBLIC_MONAD_POOL_CONFIGURATOR,
  MONAD_ORACLE: process.env.NEXT_PUBLIC_MONAD_ORACLE,
  MONAD_ACL_MANAGER: process.env.NEXT_PUBLIC_MONAD_ACL_MANAGER,
  MONAD_ACL_ADMIN: process.env.NEXT_PUBLIC_MONAD_ACL_ADMIN,
  MONAD_ATOKEN_IMPL: process.env.NEXT_PUBLIC_MONAD_ATOKEN_IMPL,
  MONAD_VARIABLE_DEBT_IMPL: process.env.NEXT_PUBLIC_MONAD_VARIABLE_DEBT_IMPL,
  MONAD_TREASURY: process.env.NEXT_PUBLIC_MONAD_TREASURY,
  MONAD_INCENTIVES_CONTROLLER: process.env.NEXT_PUBLIC_MONAD_INCENTIVES_CONTROLLER,
  MONAD_DEFAULT_INTEREST_RATE_STRATEGY:
    process.env.NEXT_PUBLIC_MONAD_DEFAULT_INTEREST_RATE_STRATEGY,
  MONAD_CONFIG_ENGINE: process.env.NEXT_PUBLIC_MONAD_CONFIG_ENGINE,
  MONAD_EMISSION_MANAGER: process.env.NEXT_PUBLIC_MONAD_EMISSION_MANAGER,
  MONAD_K613_PROTOCOL_DATA_PROVIDER: process.env.NEXT_PUBLIC_MONAD_K613_PROTOCOL_DATA_PROVIDER,
  MONAD_WALLET_BALANCE_PROVIDER: process.env.NEXT_PUBLIC_MONAD_WALLET_BALANCE_PROVIDER,
  MONAD_UI_POOL_DATA_PROVIDER: process.env.NEXT_PUBLIC_MONAD_UI_POOL_DATA_PROVIDER,
  MONAD_UI_INCENTIVE_DATA_PROVIDER: process.env.NEXT_PUBLIC_MONAD_UI_INCENTIVE_DATA_PROVIDER,
  MONAD_WETH_GATEWAY: process.env.NEXT_PUBLIC_MONAD_WETH_GATEWAY,
  MONAD_L2_ENCODER: process.env.NEXT_PUBLIC_MONAD_L2_ENCODER,
  MONAD_COLLECTOR: process.env.NEXT_PUBLIC_MONAD_COLLECTOR,
  MONAD_STAKING: process.env.NEXT_PUBLIC_MONAD_STAKING,
  MONAD_REWARDS_DISTRIBUTION: process.env.NEXT_PUBLIC_MONAD_REWARDS_DISTRIBUTION,
};

const env = cleanEnv(rawEnv, {
  NODE_ENV: str({ choices: ['production', 'development'], default: 'development' }),
  API_BASEURL: url({ default: '' }),
  SUBGRAPH_MONAD_URL: url({ default: '' }),

  MONAD_POOL_ADDRESSES_PROVIDER: str({ default: '' }),
  MONAD_POOL: str({ default: '' }),
  MONAD_POOL_CONFIGURATOR: str({ default: '' }),
  MONAD_ORACLE: str({ default: '' }),
  MONAD_ACL_MANAGER: str({ default: '' }),
  MONAD_ACL_ADMIN: str({ default: '' }),
  MONAD_ATOKEN_IMPL: str({ default: '' }),
  MONAD_VARIABLE_DEBT_IMPL: str({ default: '' }),
  MONAD_TREASURY: str({ default: '' }),
  MONAD_INCENTIVES_CONTROLLER: str({ default: '' }),
  MONAD_DEFAULT_INTEREST_RATE_STRATEGY: str({ default: '' }),
  MONAD_CONFIG_ENGINE: str({ default: '' }),
  MONAD_EMISSION_MANAGER: str({ default: '' }),
  MONAD_K613_PROTOCOL_DATA_PROVIDER: str({ default: '' }),
  MONAD_WALLET_BALANCE_PROVIDER: str({ default: '' }),
  MONAD_UI_POOL_DATA_PROVIDER: str({ default: '' }),
  MONAD_UI_INCENTIVE_DATA_PROVIDER: str({ default: '' }),
  MONAD_WETH_GATEWAY: str({ default: '' }),
  MONAD_L2_ENCODER: str({ default: '' }),
  MONAD_COLLECTOR: str({ default: '' }),
  MONAD_STAKING: str({ default: '' }),
  MONAD_REWARDS_DISTRIBUTION: str({ default: '' }),
});

export const {
  NODE_ENV,
  API_BASEURL,
  SUBGRAPH_MONAD_URL,
  MONAD_POOL_ADDRESSES_PROVIDER,
  MONAD_POOL,
  MONAD_POOL_CONFIGURATOR,
  MONAD_ORACLE,
  MONAD_ACL_MANAGER,
  MONAD_ACL_ADMIN,
  MONAD_ATOKEN_IMPL,
  MONAD_VARIABLE_DEBT_IMPL,
  MONAD_TREASURY,
  MONAD_INCENTIVES_CONTROLLER,
  MONAD_DEFAULT_INTEREST_RATE_STRATEGY,
  MONAD_CONFIG_ENGINE,
  MONAD_EMISSION_MANAGER,
  MONAD_K613_PROTOCOL_DATA_PROVIDER,
  MONAD_WALLET_BALANCE_PROVIDER,
  MONAD_UI_POOL_DATA_PROVIDER,
  MONAD_UI_INCENTIVE_DATA_PROVIDER,
  MONAD_WETH_GATEWAY,
  MONAD_L2_ENCODER,
  MONAD_COLLECTOR,
  MONAD_STAKING,
  MONAD_REWARDS_DISTRIBUTION,
} = env;

export const IS_PRODUCTION = NODE_ENV === 'production';
